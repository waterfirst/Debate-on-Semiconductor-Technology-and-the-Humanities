---
name: quarto-korean-book-publishing
description: Quarto 원고를 한국어 A5 전자책·POD 교정쇄로 편집하고, 장별 삽화·도표·날개표지·PDF·EPUB을 생성해 시각 검수하고 교보문고 제출판을 관리하는 재현 가능한 출판 워크플로입니다. ISBN, 바코드 또는 쪽수 변경 뒤 판본을 다시 만들 때도 사용합니다.
---

# Quarto 한국어 1인 출판

이 지침은 `book/_quarto.yml`이 있는 한국어 단행본을 HTML, EPUB, A5 PDF와 POD 펼침표지로 완성할 때 사용합니다.

## 1. 판형과 판본을 먼저 잠급니다

- 제목, 부제, 저자, 발행처, 장 순서를 확정합니다.
- POD 판형을 A5 148 × 210mm로 고정합니다.
- 본문 페이지 수가 확정되기 전에는 책등 폭을 최종값으로 표시하지 않습니다.
- 책날개를 쓰면 A5 기준 좌우 날개 폭을 80mm로 잡고, 재단 밖 사방 3mm 도련과 표지-날개 사이 3mm 접지 여유를 제작처 도면에 맞춰 확인합니다.
- ISBN, 가격, 발행일이 미정이면 판권면에 임의 값을 쓰지 않습니다.

## 2. 원고 구조를 통일합니다

각 장은 다음 순서를 사용합니다.

1. 오늘의 책문
2. 왜 지금 이 질문인가
3. 데이터 렌즈와 근거 표
4. 이 숫자를 읽는 법
5. 대립 답안 A·B·C
6. AI 조사 설계: 프롬프트·데이터 취득·정보 판정·토론의 핵심
7. 신입사원이 실제로 선택해야 하는 사례형 토론
8. 90초 모범 발언
9. 장에만 해당하는 꼬리 질문 6개와 직접 출처

같은 체크리스트나 면접 메모를 장마다 반복하지 말고 책 뒤에 한 번만 둡니다. 서술 종결은 존댓말로 통일하되 직접 인용은 원문을 보존합니다.

책문은 출처 아카이브의 개별 항목과 연결합니다. 원자료에서 확인한 문장만 원문이라고 부르고, 뜻을 줄여 새로 쓴 한문은 반드시 `한문 재구성` 또는 `원문 직역 아님`이라고 표시합니다. 현대적 질문은 역사적 책문의 실제 내용이 아니라 저자의 연결임을 각주에서 구분합니다.

실제 응시자 답안을 현대 답안에 연결할 때는 인물, 답안 내용, 시험 등위를 서로 독립적으로 확인합니다. 확인된 사고법만 90초 발언에 한 문장으로 의역하고, 각주에 응시자·정확한 등위·의역임을 밝힌 뒤 실록·방목·문집·기관 자료의 직접 URL을 적습니다. 장원 여부가 확인되지 않으면 장원이라고 부르지 않으며, 역사 답안은 현대 정책의 권위가 아니라 판단 순서를 비추는 선례로만 사용합니다.

## 3. 한글 조판을 설정합니다

- 본문은 Pretendard Regular 10~10.5pt를 기본으로 합니다.
- 질문이나 역사적 인용은 KoPub바탕으로 대비합니다.
- 본문에 흩어진 한문은 Lua 필터로 Noto Serif KR에 자동 연결하고, 렌더 전에 원고의 모든 한자 코드포인트가 해당 글꼴에 있는지 검사합니다.
- 줄 간격은 1.28~1.38에서 실제 A5 페이지를 보며 결정합니다.
- 한국어가 포함된 문장을 코드 서체로 감싸지 않습니다.
- 고정 단어 간격과 한국어 줄바꿈을 사용하고 과부·고아줄을 방지합니다.
- 장 제목은 수동 숫자를 넣지 않고 Quarto 번호만 사용합니다.
- 에필로그, 체크리스트, 저자 소개, 참고문헌은 장 번호를 끕니다.

## 4. 데이터와 시각자료를 편집합니다

- 수치에는 단위, 기준 연도, 출처를 표시합니다.
- 저자의 다른 글이나 개인 경험을 객관적 사실의 출처로 사용하지 않고 공공기관·학술·기업 원자료로 대체합니다.
- 서로 정의가 다른 숫자는 같은 축에서 직접 비교하지 않습니다.
- 시간 자료는 추세선, 구성비는 도넛, 범주 비교는 세로 막대나 롤리팝을 우선합니다.
- 정성적 질문은 억지 점수 대신 2×2 판단 매트릭스나 의사결정 흐름도를 사용합니다.
- 2열 표의 식별 열은 약 28%, 설명 열은 약 72%에서 시작합니다. `순서/자료/확인항목` 3열 표는 A5에서 약 13%/34%/53%로 배분해 긴 확인항목의 줄바꿈을 줄입니다.

## 5. 장별 삽화를 만듭니다

- 한 책에서는 화풍, 종이 질감, 선 굵기, 명암 범위를 고정합니다.
- 이 프로젝트는 단색 연필 데생과 옅은 수묵 번짐, 한지 바탕을 사용합니다.
- 장별 개념을 한두 개의 상징으로 줄이고 작은 크기에서도 알아보게 합니다.
- 이미지 안에 문자, 로고, 워터마크, 서명은 생성하지 않습니다.
- AI 생성 이미지는 저자가 기획·선정·수정하고 판권면에 한 번 고지합니다.
- 투명 PNG를 흑백 인쇄본으로 바꿀 때는 먼저 흰 종이 배경에 알파 합성해 둥근 모서리가 검은 쐐기로 변하지 않게 합니다.

## 6. 표지를 만듭니다

- 앞표지는 썸네일 크기에서 제목이 먼저 읽혀야 합니다.
- 상징 이미지는 하나만 크게 사용하고 불필요한 선과 장식을 제거합니다.
- 저자 요청에 따라 앞표지에는 저자명과 발행처를 생략할 수 있으나 판권면에는 정확히 표시합니다.
- 발행처 로고는 앞표지의 주목도를 해치지 않도록 뒤표지 왼쪽 아래에 두고, ISBN 바코드 안전 영역은 오른쪽 아래에 둡니다.
- 좁은 책등에는 전체 가로 로고 대신 출판사 워드마크를 작게 둡니다.
- 판권면과 전자책 메타데이터에는 `스칼라브릿지(Scholar Bridge)`처럼 한글·영문 정식 발행처명을 기록합니다.
- 책날개를 쓰는 첫 책이라면 한쪽에는 지원 독자를 위한 상징 이미지와 출간 취지를, 다른 쪽에는 저자의 짧은 당부와 이름을 둡니다.
- 책등은 본문 쪽수와 종이 두께로 계산합니다. 백색모조 100g을 0.12mm로 가정하면 `ceil(ceil(쪽수/2) × 0.12)`mm이며, 이 판본의 258쪽은 16mm입니다. 최종값은 유통사 자동 계산값이나 인쇄소 실측값을 우선합니다.
- 펼침표지는 `왼쪽 날개 + 뒤표지 + 책등 + 앞표지 + 오른쪽 날개` 순으로 만들고, 사방 3mm 도련을 포함한 MediaBox/BleedBox와 재단 크기의 TrimBox를 명시합니다.
- 책등 폭은 유통사가 정한 종이와 페이지 계산식으로 다시 산정합니다.

## 7. 세 번 검수합니다

### 구조 검수

- `_quarto.yml`에 선별된 출간 장과 필수 섹션이 모두 있는지 확인합니다.
- 존댓말 혼용, 반복 코너, 수동 장 번호, 깨진 문자와 빈 이미지 링크를 검색합니다.
- 서문·에필로그·체크리스트·저자 소개는 `.unnumbered`로 두고, 부 제목에는 Quarto가 붙일 번호를 수동으로 다시 쓰지 않습니다.

### 시각 검수

- PDF를 페이지 이미지로 렌더링합니다.
- 서문, 긴 장 제목, 첫 장, 중간 장, 마지막 장, 표, 도형, 체크리스트, 저자 소개를 반드시 봅니다.
- 한 글자 고아줄, 과도한 단어 간격, 표 머리글 세로 분리, 이미지 치우침, 캡션 중복을 수정합니다.
- 한문 각주가 네모 상자로 출력되지 않는지 원본 크기로 확대해 확인합니다.

### 산출물 검수

- 본문 PDF 페이지 수와 A5 크기를 확인합니다.
- 완성본은 `앞표지 + 본문 + 뒷표지` 페이지 수가 맞아야 합니다.
- 펼침표지는 1쪽이어야 하며 책날개 판본은 `왼쪽 날개 + 뒤표지 + 책등 + 앞표지 + 오른쪽 날개 + 도련` 순서여야 합니다.
- PDF 메타데이터의 제목·저자·발행처를 확인합니다.

## 8. 추천사·서평 없는 초판을 확정합니다

- 이 판본에는 추천사·서평과 관련 빈 페이지를 넣지 않습니다.
- 판권면, 목차, 렛유인 검토본에도 추천사 수록 예정 문구를 넣지 않습니다.
- 판매 승인 뒤 쪽수를 바꾸면 개정판과 새 ISBN이 필요할 수 있으므로 현재 내지·책등·표지를 하나의 고정 판본으로 관리합니다.

## 9. 렌더링 순서

```powershell
python scripts/audit_manuscript.py
python scripts/check_source_links.py
python scripts/audit_hanja_fonts.py
python scripts/enrich_content.py
node scripts/render_chapter_figures.mjs
python scripts/prepare_print_images.py
python scripts/activate_print_images.py
node scripts/render_cover_assets.mjs
cd book
quarto render . --to epub
Copy-Item "_book/*.epub" "output/epub/" -Force
quarto render . --to pdf
python cover/build_cover_pdfs.py
cd ..
uv run --with pypdf python scripts/verify_publish_artifacts.py
python scripts/build_recommendation_packets.py
python scripts/build_letyuin_review_packet.py
```

Quarto는 형식별 렌더링 때 `_book`을 갱신하므로 EPUB을 안정된 출력 폴더에 복사한 뒤 PDF를 마지막에 렌더링합니다. PDF 본문 페이지 수가 바뀌면 펼침표지의 책등 폭과 SVG 캔버스를 다시 계산한 뒤 표지를 재렌더링합니다.

## 10. 유통사 업로드 직전

- POD 본문 PDF와 펼침표지 PDF를 서로 다른 파일로 준비합니다.
- EPUB은 목차, 표, 이미지, 각주와 화면 크기별 줄바꿈을 미리보기에서 확인합니다.
- ISBN, 가격, 발행일, 바코드, AI 활용 고지를 확정합니다.
- Scholar Bridge 출판사 계정에서는 종이책과 전자책 ISBN을 각각 발급하고 납본 책임을 확인합니다.
- 생성형 AI를 기획, 원고, 편집, 번역 또는 이미지에 사용했다면 유통사 등록 화면의 AI 활용 여부를 사실대로 선택합니다.
- 이 프로젝트의 교보 제출 경로와 최신 확인 항목은 `KYOBO_PUBLISHING_GUIDE.md`를 읽습니다.
- 최종 업로드는 유통사가 제공하는 최신 템플릿과 규격을 우선합니다.
